// comments support

// if statements
2 2 + 4 == if
    // een code block
else
    // een code block
end

// loops
while <condition> do
    // een code block
end

for <range_start> <range_end> do
    // een code block
end

// functies?
func name | <type1> <type2> ... | do
    // een code block
end

// constants
const <waarden> end