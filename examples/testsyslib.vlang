import "stdlib/io.vlang";

@macro BUFSZ 5

byte buffer[BUFSZ];
long bytes_read = read(STDIN, buffer, BUFSZ-1);
buffer[bytes_read] = 0;
printstr("Read ");
printint(bytes_read);
println(" bytes.");

printstr("Buffer: ");
println(buffer);

_clear_input(STDIN);